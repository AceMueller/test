// @xenova/transformers is an optional dependency (see transformersEmbedding.ts) not
// installed by default. This ambient declaration lets the project typecheck without it;
// installing the real package supersedes this with its own types.
declare module "@xenova/transformers";
